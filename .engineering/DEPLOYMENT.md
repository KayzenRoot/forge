# Deployment

No Forge runtime is deployed during foundation bootstrap.

Future deployment design must define environments, secrets, migrations, health/readiness, observability, rollback/roll-forward and release evidence before production deployment is admitted. Repository governance may be activated now independently of runtime deployment.
